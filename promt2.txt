I will paste a lab/project question (with versions v1, v2, v3, v4).

Your job is to convert it into a **complete, extremely detailed, step-by-step execution guide**.

---

# 1. CORE EXECUTION FLOW

👉 Always follow this base flow:

**VS Code → GitHub → Jenkins Pipeline (Build) → Verification**

---

# 2. STRICT QUESTION MATCHING RULE (VERY IMPORTANT)

You MUST follow the question **exactly as written**.

* Do NOT rename parameters unless absolutely necessary
* Do NOT modify task names
* Do NOT change variable names

### If a real-world conflict exists (IMPORTANT):

Example:

* Question says `BUILD_ID`
* Jenkins already has internal `BUILD_ID`

👉 Then you MUST:

1. Use the **exact name from the question**
2. Add a note:

   > “In real-world usage, this may conflict with Jenkins internal variables. Using BUILD_ID_PARAM is safer, but here we follow the question exactly.”

---

# 3. PARAMETER HANDLING RULE (CRITICAL)

If question says:

### “String parameter”

```groovy
string(name: 'PARAM_NAME', ...)
```

### “Numeric parameter”

👉 Jenkins declarative pipeline does NOT have a strict numeric type

So you MUST:

* Use `string` parameter
* Add note:

  > “Jenkins uses string input; user should enter numeric values only”

---

# 4. “ALL PARAMETERS” RULE

If question says:

> “Display all parameter values”

👉 You MUST clarify:

* Either:

  * “All parameters in THIS version”
* OR:

  * “All parameters introduced so far”

❗ If unclear → assume:
👉 “All parameters defined in current version”

---

# 5. SEPARATE BUILD RULE (CRITICAL)

Each version MUST be executed as a **separate Jenkins build**.

For EVERY version, you MUST explicitly say:

> “Click Build Now — this runs as a separate Jenkins build”

❗ Never assume:

* v2 continues from v1
* v3 includes v1

---

# 6. WORKSPACE HANDLING RULE (CRITICAL)

Jenkins workspace may:

* persist
  OR
* reset

So ALWAYS include:

> “Jenkins may reuse or clean workspace. Do not assume previous files exist.”

### Enforcement:

If files are needed:

* recreate OR overwrite safely

Example:

```bat
echo data > file.txt   (safe overwrite)
echo data >> file.txt  (append)
```

❗ VERY IMPORTANT:

* If question says “append” → MUST use `>>`
* If question says “create/save” → use `>`

---

# 7. GIT RULES (STRICT)

### Initial Setup

```bash
git init
git branch -M main
git remote add origin <repo-url>
git add .
git commit -m "Initial commit"
git push -u origin main
```

### Version Branching

#### v1

```bash
git checkout main
git pull origin main
git checkout -b v1
git push -u origin v1
```

#### v2

```bash
git checkout v1
git pull origin v1
git checkout -b v2
git push -u origin v2
```

#### v3

```bash
git checkout v2
git pull origin v2
git checkout -b v3
git push -u origin v3
```

#### v4

```bash
git checkout v3
git pull origin v3
git checkout -b v4
git push -u origin v4
```

---

# 8. JENKINS RULES

For EACH version:

1. Open Jenkins → `http://localhost:8080`
2. Open Pipeline Job
3. Click Configure
4. Replace script
5. Save
6. Click **Build Now**

👉 MUST explicitly say:

> “This version runs as a separate build”

---

# 9. PIPELINE CODE RULES

* ALWAYS give FULL pipeline code
* NEVER say “same as previous”
* NEVER skip code

---

## Windows Jenkins

Use:

```groovy
bat 'command'
```

---

# 10. FILE HANDLING RULE

Use:

Create:

```
echo text > file.txt
```

Append:

```
echo text >> file.txt
```

Display:

```
type file.txt
```

Explain:

* `>` overwrites
* `>>` appends

---

# 11. METADATA RULE

Use:

* `${env.BUILD_NUMBER}`
* `${env.JOB_NAME}`
* `${env.NODE_NAME}`
* `${env.WORKSPACE}`

---

# 12. TIMESTAMP RULE

Use readable format:

```bat
echo Build Timestamp: %DATE% %TIME% >> file.txt
```

---

# 13. CONDITIONAL DEVOPS RULE

ONLY include Docker/Kubernetes if explicitly asked.

---

# 14. REQUIRED OUTPUT FORMAT

For EACH version:

## Version X – Feature Name

### 1. Tasks

### 2. Git Commands

### 3. Jenkins Steps

### 4. Jenkins Pipeline Code (FULL)

### 5. Explanation

### 6. Workspace Handling Note

### 7. Verification

### 8. Common Errors + Fix

---

# 15. FINAL SUMMARY

Include:

* what each version achieved
* parameter usage explanation
* workspace handling explanation
* key learning points

---

# 16. STRICT RULES

* ALWAYS match parameter names exactly from question
* ALWAYS explain numeric parameter handling
* ALWAYS clarify “all parameters” meaning
* ALWAYS use `>` vs `>>` correctly
* ALWAYS run versions separately
* ALWAYS include full code
* NEVER assume previous build output exists
* NEVER modify question logic

---

Now wait for the question and generate the full execution guide.

[PASTE QUESTION HERE]
