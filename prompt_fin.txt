I will paste a project/lab question where features are given version-wise like:

Project Name
v1 (Feature + Description)
v2 (Feature + Description)
v3 (Feature + Description)

Your job is to convert it into a **complete, extremely detailed, real execution guide** following this EXACT flow for **each version separately**:

👉 **VS Code → Local Run → GitHub → Jenkins Pipeline → Docker build → DockerHub push → Kubernetes deploy → Verification**

Very important:

* Each version must be **built, pushed, deployed, and verified separately** in incremental order
* First complete **v1 fully**
* Then complete **v2 fully**, based on **v1**
* Then complete **v3 fully**, based on **v2**
* Do **NOT** combine all versions into one final build
* Do **NOT** skip deploying any version
* Do **NOT** assume building v3 automatically covers v1 and v2
* For each version, explicitly show that **Jenkins is run separately** and **Kubernetes is updated separately**
* Focus on **real execution**, not theory only

Follow all rules below STRICTLY.

---

# 1. Understanding Rules

* Treat each version as **incremental development**
* v2 builds on v1
* v3 builds on v2
* NEVER rewrite from scratch unless the question explicitly says so
* ALWAYS explain what changed from the previous version

---

# 2. CRITICAL EXECUTION RULES (MUST FOLLOW)

## A. Environment Rule

Assume the setup is **Windows-based** unless the question explicitly says Linux.

Therefore:

* In Jenkins Pipeline on Windows, use **`bat`**
* Do **NOT** use `sh` unless the environment is explicitly Linux/WSL

If the environment could vary, clearly mention:

* `bat` for Windows Jenkins
* `sh` for Linux Jenkins

But default to **Windows + bat**

---

## B. Git Rules

Use this exact branching logic:

### Initial setup

```bash
git init
git branch -M main
git remote add origin <repo-url>
git add .
git commit -m "Initial commit"
git push -u origin main
```

### v1

```bash
git checkout main
git pull origin main
git checkout -b v1
git add .
git commit -m "Added v1 feature"
git push -u origin v1
```

### v2

```bash
git checkout v1
git pull origin v1
git checkout -b v2
git add .
git commit -m "Added v2 feature"
git push -u origin v2
```

### v3

```bash
git checkout v2
git pull origin v2
git checkout -b v3
git add .
git commit -m "Added v3 feature"
git push -u origin v3
```

Never:

* skip pushing `main`
* skip `git pull`
* create v2 directly from main unless explicitly required
* create v3 directly from main unless explicitly required

---

## C. File Creation Rule

Before pushing any version, make sure **all required files for that version already exist** in the project folder, such as:

* app.py or main program file
* requirements.txt
* Dockerfile
* deployment.yaml
* service.yaml

If files are missing before Git push, Jenkins checkout/build/deploy may fail.

Very important:

* **Show the actual file contents first**
* Then do the **Required Files Check**
* Then do the **Git push**
* Do **NOT** mention deployment.yaml and service.yaml in the file check before actually showing them

---

## D. Separate Build and Deployment Rule

For **every version**, explicitly show this full cycle separately:

### v1 cycle

1. Write/update code in VS Code
2. Test locally
3. Confirm all files exist
4. Push v1 branch to GitHub
5. Run Jenkins pipeline for v1
6. Build Docker image `:v1`
7. Push Docker image `:v1` to DockerHub
8. Deploy `:v1` to Kubernetes
9. Verify v1 is working

### v2 cycle

1. Start from v1 code
2. Add v2 feature
3. Test locally
4. Confirm all files exist
5. Push v2 branch to GitHub
6. Run Jenkins pipeline for v2
7. Build Docker image `:v2`
8. Push Docker image `:v2` to DockerHub
9. Deploy `:v2` to Kubernetes
10. Verify v2 is working

### v3 cycle

1. Start from v2 code
2. Add v3 feature
3. Test locally
4. Confirm all files exist
5. Push v3 branch to GitHub
6. Run Jenkins pipeline for v3
7. Build Docker image `:v3`
8. Push Docker image `:v3` to DockerHub
9. Deploy `:v3` to Kubernetes
10. Verify v3 is working

You MUST make this separation very explicit.

---

## E. DockerHub Consistency Rule

The Docker image name must match everywhere:

* Jenkins pipeline
* Docker build command
* Docker push command
* deployment.yaml

Example:

```text
yourdockerhubusername/app:v1
```

You MUST clearly say:
**Replace all occurrences of `yourdockerhubusername` and `<repo-url>` everywhere in every version.**

---

## F. Jenkins Rule

For each version, the Jenkins pipeline must:

1. Checkout the correct branch
2. Build the correct Docker image tag
3. Login to DockerHub using Jenkins credentials
4. Push the image
5. Deploy to Kubernetes
6. Restart rollout if needed

Very important:

* Give the **full Jenkins Pipeline code for each version**
* Do **NOT** just say “update the old pipeline”
* Show the complete code block for v1, v2, and v3 separately

---

## G. Tool Verification Rule

Before starting, include these checks:

```bash
docker --version
docker ps
kubectl version --client
kubectl get nodes
```

Explain what each one verifies.

---

## H. Kubernetes Update Rule

For every deployment update, include:

```bash
kubectl rollout restart deployment <deployment-name>
```

Explain why:

* ensures the new image version is pulled and deployed

---

## I. Multi-Version Deployment Rule

Because each version is deployed separately, explain one of these clearly:

### Option 1: Separate services and ports

* app-service-v1 → NodePort 30001
* app-service-v2 → NodePort 30002
* app-service-v3 → NodePort 30003

### Option 2: Same service overwritten version by version

If using this, explain it clearly and consistently.

Prefer **Option 1** for clarity in labs and demos.

Also clarify whether deployment names are:

* different for each version
  or
* reused and restarted

Be fully consistent.

---

## J. Docker Images Verification Rule

Explain clearly:

* `docker images` will show all versions only if Jenkins build is run separately for v1, v2, and v3
* building only v3 does **NOT** automatically create v1 and v2 locally

---

# 3. REQUIRED OUTPUT FORMAT (STRICT)

For **EACH version**, use EXACTLY this structure:

---

## Version X – Feature Name

### 1. Feature Explanation

* What the version does
* What changed from previous version

### 2. VS Code Implementation

* Exact step-by-step file creation or update
* Mention exact file names
* Give full code, copy-paste ready
* Show full project structure if needed

### 3. Local Testing

* Exact run command
* URL to open
* Expected output

### 4. Kubernetes YAML for This Version

Give full:

* `deployment.yaml`
* `service.yaml`

Make sure:

* image tag is correct for this version
* deployment name logic is clear
* service name is clear
* NodePort is correct for this version

### 5. Required Files Check

After showing **all files**, explicitly confirm:

* app.py
* requirements.txt
* Dockerfile
* deployment.yaml
* service.yaml

### 6. GitHub Push

* Exact Git commands for that version only

### 7. Jenkins Setup / Run for This Version

Explain:

* how to open Jenkins
* whether to create a new pipeline job or update existing one
* that this version must be built separately
* exactly when to click Save and Build Now

### 8. DockerHub Credentials Setup

Explain:

* Manage Jenkins → Credentials
* Add Username with password
* ID: `docker-creds`

### 9. Jenkins Pipeline Code for This Version

Give **full pipeline code for this specific version only**

It must include:

* correct branch
* correct image tag
* Docker login
* Docker push
* Kubernetes deploy

Important:

* Use **`bat`** commands for Windows Jenkins
* Do not use `sh` unless explicitly told Linux

### 10. Verification for This Version

Include:

* Jenkins success check
* `docker images`
* `kubectl get pods`
* `kubectl get services`
* browser URL for this version
* what output should appear

### 11. Common Errors + Fix

Include:

* wrong branch
* missing files
* Docker push denied
* wrong image tag
* Kubernetes old version still showing
* port conflicts
* Jenkins credentials mismatch
* using `sh` on Windows Jenkins

---

# 4. FINAL SUMMARY

At the end, include:

* how v1, v2, and v3 were built separately
* how each version was deployed separately
* full flow:
  **VS Code → GitHub → Jenkins → Docker → DockerHub → Kubernetes**
* key learning points
* short viva questions

---

# 5. STRICT RULES (DO NOT BREAK)

* ALWAYS push main first
* ALWAYS use incremental branches
* ALWAYS create all files before Git push
* ALWAYS show YAML before the required-files check
* ALWAYS build each version separately in Jenkins
* ALWAYS push each version image separately to DockerHub
* ALWAYS deploy each version separately to Kubernetes
* ALWAYS verify each version separately
* ALWAYS provide full Jenkins Pipeline code for every version
* ALWAYS provide full Kubernetes YAML for every version
* NEVER replace full code with “just update previous code” only
* NEVER use `sh` for Windows Jenkins
* NEVER merge all versions into one build unless the question explicitly asks
* NEVER skip the local testing step
* NEVER skip the verification step
* Use simple beginner-friendly language
* Focus on real execution, not theory only
* NEVER write ‘update previous pipeline’ — ALWAYS give full pipeline code per version
* NEVER write ‘update YAML’ — ALWAYS give full deployment.yaml and service.yaml per version
* ALWAYS include git pull origin <branch> before creating next version branch

---

Now wait for the question and generate the full step-by-step execution guide.

